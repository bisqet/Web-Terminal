import {css} from "lit";

export const terminalPathStyles = css`
  :host {
    display: inline-block;
    white-space: nowrap;
  }

  .spanWithDivider {
    display: inline-block;
    margin-right: 1ch;
  }
`